
allprojects {
    group = "org.kebish"
    version = "0.2.2"
    description = "Library for browser tests implementing Page Object pattern and using Selenium"

    repositories {
        mavenLocal()
        mavenCentral()
    }

}
